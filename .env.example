# Neon connection string. Use the pooled ("-pooler") connection for the app,
# and the direct connection for migrations if Neon gives you two separate URLs.
DATABASE_URL="postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/jobtracker?sslmode=require"
